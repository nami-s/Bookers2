<h1>Users</h1>
<table>
  <thead>
    <tr>
      <th>image</th>
      <th>name</th>
      <th></th>
    </tr>
  </thead>
  <% @users.each do |user| %>
  <tbody>
    <tr>
      <td><span><%= user.profile_image_id %></span></td>
      <td><span><%= user.name %></span></td>
      <td><span><%= link_to "show", user_path(user.id) %></span></td>
    </tr>
  </tbody>
    <% end %>
</table>